# Copy to .env for local development (without Docker).
# .env is gitignored — connection strings contain credentials.
DATABASE_URL=postgresql://bookuser:bookpass@localhost:5432/booksdb
PORT=5000
DB_POOL_MAX=5
